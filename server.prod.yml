runtime: nodejs
env: flex

env_variables:
  NODE_ENV: prod
  SERVICE: server.js

skip_files:
- ^\.git/.*
- ^coverage/(.*/)?
- ^node_modules/(.*/)?
- ^test/(.*/)?

handlers:
- url: /.*
  script: server
  secure: always

resources:
  cpu: 2
  memory_gb: 1.0
