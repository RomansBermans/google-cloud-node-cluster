service: default
runtime: nodejs
env: flex

env_variables:
  NODE_ENV: prod
  SERVICE: server.js

resources:
  cpu: 2
  memory_gb: 1.0

handlers:
- url: /.*
  script: server
  secure: always

skip_files:
- ^\.git/.*
- ^coverage/(.*/)?
- ^node_modules/(.*/)?
- ^test/(.*/)?
